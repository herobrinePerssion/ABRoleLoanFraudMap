# AB 贷反诈项目产品规划与开发清单

> 版本：v1.0
> 日期：2026-04-16
> 目标：为后续功能开发提供统一的产品路线、页面清单、功能优先级、里程碑与验收标准。

---

## 1. 项目定位与目标

### 1.1 产品定位
AB 贷反诈项目是一个“反诈教育 + 风险预警 + 线索举报闭环”的公众服务平台，面向潜在受害者、已受害用户与反诈运营人员。

### 1.2 核心目标
1. **认知教育**：帮助用户快速识别 AB 贷骗局特征。
2. **风险预警**：通过地图与统计数据展示高风险区域、常见话术、风险趋势。
3. **线索闭环**：实现“提交举报 → 进度可查 → 反馈可见”的可追踪流程。

### 1.3 北极星指标（North Star）
- 举报转化率：举报页访问用户中完成提交的比例。
- 查询使用率：提交后 7 天内主动查询进度的比例。
- 教育完成率：关于页关键模块阅读完成率。
- 预警触达率：首页风险提醒曝光比例。

### 1.4 业务结果指标（建议）
- 日/周新增线索数
- 有效线索率（可核验）
- 重复线索率（越低越好）
- 用户平均提交时长（越短越好）
- 首次反馈时长（SLA）

---

## 2. 用户角色与核心场景

## 2.1 用户角色
1. **普通用户**：想了解 AB 贷、识别风险。
2. **受害用户**：已发生资金损失，需要提交举报并查询进度。
3. **运营审核人员（后续）**：处理线索、标记风险、反馈进度。

### 2.2 关键场景
- 场景 A：通过首页快速识别“先收费”骗局并规避风险。
- 场景 B：提交线索后保存编号并查询处理状态。
- 场景 C：按地区/话术观察风险趋势并查看案例细节。
- 场景 D：运营人员对线索去重、打标、推进状态。

---

## 3. 信息架构（IA）与页面清单

当前已有：首页、关于、举报入口、举报成功/查询、数据统计。
建议补齐为“公众前台 + 运营后台”双层结构：

## 3.1 公众前台页面
1. **首页**（已）
   - 价值主张
   - 风险提醒
   - 地图与筛选
   - 热门案例/常见骗局入口
2. **关于 AB 贷**（已）
   - 概念定义
   - 常见流程
   - 自检清单
   - 参考资料
3. **举报入口**（已）
   - 线索表单
   - 提交注意事项
4. **举报成功与进度查询**（已）
   - 编号展示
   - 状态时间线
   - 二次查询
5. **数据统计**（已）
   - KPI
   - 话术占比
   - 地区风险
6. **防骗自测页**（新增）
   - 5~10 道题
   - 结果评分与建议
7. **案例列表页**（新增）
   - 按地区/类型筛选
   - 标签检索
8. **案例详情页**（新增）
   - 案发经过
   - 关键风险点
   - 防范建议
9. **帮助中心 / FAQ**（新增）
   - 常见问题
   - 举报流程说明
10. **法律声明与隐私政策**（新增）
    - 采集范围
    - 存储与使用
    - 用户权利

## 3.2 运营后台页面（建议后续）
1. 线索管理台（列表、筛选、去重）
2. 线索详情与审核工作台（状态推进、备注）
3. 风险标签管理（话术、类型、风险级别）
4. 数据导出与报表中心
5. 权限与账号管理

---

## 4. 功能规划（按优先级）

## P0（必须，保证可用闭环）
1. **举报表单增强**
   - 必填校验、格式校验、重复提交拦截
   - 提交成功后生成唯一编号
   - 本地/接口统一存储结构
2. **进度查询闭环**
   - 编号查询
   - 状态机：待初审 → 处理中 → 已反馈
   - 时间线记录每次处理节点
3. **风险标准化**
   - 诈骗类型、话术标签、金额区间统一枚举
4. **异常体验完善**
   - 空态、错误态、超时重试、无结果引导

## P1（增强可信度与传播）
1. 防骗自测（评分 + 建议）
2. 案例库（列表/详情）
3. 趋势统计（按日/周/月）
4. 首页内容运营位（热点预警、权威链接）
5. SEO 基础优化（标题、描述、社交分享）

## P2（规模化运营）
1. 后台审核与协作流
2. 黑名单实体库（账号、手机号、网址）
3. 多渠道线索接入（API/表单聚合）
4. 自动周报与风险播报

---

## 5. 关键用户流程（端到端）

## 5.1 举报流程
进入举报页 → 填写信息 → 提交成功 → 生成编号 → 引导保存编号 → 跳转查询页。

### 验收点
- 编号可唯一定位记录
- 重复刷新不丢失结果
- 用户可直接复制编号

## 5.2 查询流程
输入编号 → 匹配记录 → 展示当前状态 + 历史节点 → 提示下一步操作。

### 验收点
- 无记录时给出明确引导
- 状态文本与时间线一致
- 支持再次查询与返回提交页

## 5.3 风险教育流程
首页预警 → 进入关于页/案例页 → 完成自检 → 回到举报页。

### 验收点
- 每步都有明确 CTA（行动按钮）
- 页面间跳转路径短且清晰

---

## 6. 数据模型建议（前后端协同）

## 6.1 举报记录 Report
- id: string（编号，如 AB-YYYYMMDD-XXXX）
- name: string（线索主体）
- location: string（省/市）
- address: string（详细地址）
- description: string（举报描述）
- status: enum（待初审/处理中/已反馈）
- createdAt: datetime
- updates: Array<{ time, content, operator? }>
- tags: string[]（话术标签、诈骗类型）
- amountRange?: string（金额区间）

## 6.2 风险枚举（统一字典）
- 诈骗类型：保证金诈骗、解冻金诈骗、刷流水诈骗、征信修复诈骗等
- 风险等级：低 / 中 / 高
- 处理状态：待初审 / 处理中 / 已反馈 / 已归档

---

## 7. 埋点与数据看板（建议）

## 7.1 前台埋点事件
- page_view_home
- page_view_about
- page_view_report
- click_submit_report
- submit_report_success
- click_query_report
- query_report_success
- query_report_not_found
- click_risk_case
- complete_quiz

## 7.2 看板指标
- 页面访问 UV/PV
- 举报页转化漏斗
- 查询成功率
- 无结果查询率
- 自测完成率与高风险人群占比

---

## 8. 安全、合规与风控要求

1. **最小化采集**：只采集处理线索必要信息。
2. **敏感信息保护**：脱敏展示，严禁采集密码、验证码。
3. **隐私声明**：提供可访问的隐私政策与免责声明。
4. **防刷策略**：同设备频控、验证码（后续接入）。
5. **审计追踪**：状态变更保留时间与操作者。

---

## 9. 技术实施建议（结合现有 Vue3 + Vite）

1. 新增 `src/views` 页面：Quiz、Cases、CaseDetail、FAQ、Policy。
2. 新增 `src/components`：
   - `StatusTimeline`
   - `RiskTagCloud`
   - `CaseCard`
   - `EmptyState`
3. 新增 `src/constants`：字典枚举与状态映射。
4. 新增 `src/services`：接口封装（当前可先 mock）。
5. 新增 `src/types`：Report / Case / Metrics 类型定义。
6. 统一错误处理与提示策略（消息文案规范）。

---

## 10. 四周开发里程碑（可执行）

## 第 1 周（P0 基础闭环）
- 表单校验完善
- 编号与状态机稳定
- 查询页异常态补齐
- 关键埋点接入

## 第 2 周（P0 体验与稳定）
- 表单交互优化
- 空态/错误态/重试策略
- 数据结构标准化
- 端到端回归测试

## 第 3 周（P1 内容能力）
- 防骗自测页
- 案例列表与详情
- 统计页趋势模块

## 第 4 周（P1 发布准备）
- 帮助中心 + 隐私政策
- SEO 与性能优化
- 发布检查与复盘

---

## 11. 任务看板模板（建议复制到项目管理工具）

| ID | 模块 | 任务 | 优先级 | 负责人 | 状态 | 截止日期 | 验收标准 |
|---|---|---|---|---|---|---|---|
| T001 | 举报 | 表单校验规则完善 | P0 |  | 待开始 |  | 必填/格式/错误提示完整 |
| T002 | 举报 | 编号生成与去重 | P0 |  | 待开始 |  | 编号唯一可追踪 |
| T003 | 查询 | 状态时间线组件 | P0 |  | 待开始 |  | 状态与时间节点一致 |
| T004 | 内容 | 防骗自测页 | P1 |  | 待开始 |  | 完成评分与建议展示 |
| T005 | 内容 | 案例列表页 | P1 |  | 待开始 |  | 支持筛选与分页 |
| T006 | 内容 | 案例详情页 | P1 |  | 待开始 |  | 风险点结构化展示 |
| T007 | 合规 | 隐私政策页 | P1 |  | 待开始 |  | 可访问且内容完整 |
| T008 | 数据 | 埋点看板 | P1 |  | 待开始 |  | 核心漏斗指标可视化 |
| T009 | 后台 | 线索管理台（一期） | P2 |  | 待开始 |  | 可筛选、可状态推进 |

---

## 12. 页面级验收标准（Definition of Done）

每个页面上线前需满足：
1. 主流程可用，异常态完整。
2. 关键行为已埋点。
3. 文案统一，CTA 清晰。
4. 响应式适配（移动端/桌面端）。
5. 基础可访问性（键盘、对比度、语义）。
6. 构建通过、无阻塞型错误。

---

## 13. 风险与应对

1. **线索真实性参差**：增加证据字段与审核流程。
2. **重复提交过多**：做去重策略与频控。
3. **查询被滥用**：后续引入二次验证（手机号后四位/验证码）。
4. **数据口径不一致**：建立统一字典与统计口径文档。

---

## 14. 下一步建议（立即可执行）

1. 先冻结 P0 范围（不再扩项）。
2. 先实现“防骗自测页 + 案例列表/详情”作为下一阶段重点。
3. 同步建立任务看板并按周复盘。
4. 每周固定更新指标（提交数、查询率、教育完成率）。

---

## 15. 附：建议新增路由草案

- /quiz（防骗自测）
- /cases（案例列表）
- /cases/:id（案例详情）
- /help（帮助中心）
- /policy（隐私与法律）
- /admin/reports（运营后台一期）

> 注：后台可后续独立项目或同仓库分模块实现。

---

## 16. 单人研发视角下的规划完善项（重点补充）

以下内容用于将现有规划从“团队协作版本”收敛为“单人可执行版本”。

### 16.1 MLP 边界（本期必须聚焦）
本期只保留 1 条主链路：

识别风险（首页/关于） → 提交线索（举报页） → 查询进度（成功页）

非主链路功能（案例库深度运营、后台权限体系、多渠道接入）统一后置。

### 16.2 非目标清单（防止范围蔓延）
本阶段明确不做：
1. 完整后台权限系统（RBAC）
2. 多端账号体系（短信登录、三方登录）
3. 复杂工作流（自动派单、多角色审批）
4. 大规模实时分析（流式计算）

### 16.3 指标目标值（可量化）
建议先设定保守目标值：
1. 举报转化率 ≥ 8%
2. 查询成功率 ≥ 90%
3. 无结果查询率 ≤ 15%
4. 首次反馈时长（演示环境）≤ 72 小时

### 16.4 用户任务优先级规则
采用优先级公式：

优先级分数 = 高频程度 × 风险影响 × 实现成本系数（成本越低系数越高）

优先做“高频 + 高风险 + 低成本”任务。

### 16.5 内容可信度机制
每条内容建议增加：
1. 来源类型（官方/媒体/用户线索）
2. 更新时间（yyyy-mm-dd）
3. 可信级别（高/中/低）

### 16.6 线索质量与去重口径
建议增加定义：
1. 有效线索：主体信息 + 关键事实 + 可验证证据其二
2. 重复线索：主体 + 时间窗口 + 地址/联系方式高度相似
3. 无效线索：信息缺失严重、明显虚构、与主题无关

### 16.7 合规与应急机制（轻量版）
建议补充流程：
1. 恶意举报处理：标记 + 隐藏 + 留痕
2. 误伤申诉入口：邮件/表单
3. 隐私投诉响应时限：48 小时内回应

### 16.8 发布门槛（单人版）
每次发布必须满足：
1. 主链路可走通
2. 提交与查询埋点正常
3. 无 P0 缺陷
4. 可回滚（保留上一个稳定构建）

### 16.9 运营节奏（每周固定动作）
1. 更新 1~2 条风险案例
2. 复盘提交/查询数据
3. 优化 1 处关键页面文案（CTA/提示语）

### 16.10 单人执行看板（建议长期使用）
每周只维护三类任务：
1. 本周必做（最多 3 项）
2. 本周可做（最多 2 项）
3. 下周候选（不限）

---

## 17. 单人研发两周冲刺模板（可直接执行）

### 第 1 周（闭环稳定）
1. 完善举报字段校验与错误提示
2. 固化编号查询与状态展示
3. 补齐空态/异常态

### 第 2 周（可信增强）
1. 新增防骗自测页（最小版本）
2. 新增案例列表页（静态数据可先行）
3. 增加内容来源与更新时间字段

### 两周结束验收
1. 主链路可用
2. 指标可追踪
3. 下个迭代输入明确
