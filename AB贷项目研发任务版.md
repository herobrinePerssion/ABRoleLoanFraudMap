# AB 贷项目研发任务版（前端 / 后端 / 测试）

> 版本：v1.0
> 日期：2026-04-16
> 目的：将产品规划转化为可执行研发任务，便于排期、分工、跟踪与验收。

---

## 1. 研发范围（本期）

本期聚焦 P0 + 部分 P1：
1. 举报闭环（提交、编号、查询、状态流转）
2. 公共页面增强（防骗教育、案例模块预留）
3. 数据与埋点基础能力
4. 合规基础页面与最小风控

---

## 2. 团队分工建议

- 前端：页面、交互、组件、埋点、错误处理
- 后端：接口、存储、审核状态流、鉴权与风控
- 测试：功能、接口、兼容、回归、性能与安全基线

---

## 3. 任务分解（按角色）

## 3.1 前端任务清单

### FE-P0-01 举报表单完善
- 任务内容：
  - 字段校验（必填、长度、格式）
  - 重复点击提交保护（按钮 loading、幂等提示）
  - 错误提示统一
- 关联页面：/report
- 输出物：
  - 表单校验规则
  - 失败态与成功态 UI
- 验收标准：
  - 非法输入不可提交
  - 提交中不可重复触发
  - 错误提示可理解、可定位

### FE-P0-02 提交成功页与查询页闭环
- 任务内容：
  - 成功页展示编号、状态、时间线
  - 编号查询与无结果态
  - 编号复制与返回提交入口
- 关联页面：/report/success
- 输出物：
  - 状态展示组件
  - 查询交互逻辑
- 验收标准：
  - 可通过编号检索记录
  - 无记录时有明确引导
  - 时间线与状态一致

### FE-P0-03 状态与字典前端标准化
- 任务内容：
  - 抽离状态枚举与文案映射
  - 统一 Tag 颜色映射
- 输出物：
  - constants/status.ts
  - constants/dict.ts
- 验收标准：
  - 页面不再出现硬编码状态文案

### FE-P0-04 异常与空态统一
- 任务内容：
  - 空数据、查询失败、超时重试组件化
- 输出物：
  - EmptyState / ErrorState 组件
- 验收标准：
  - 各关键页面异常体验一致

### FE-P1-01 防骗自测页（一期）
- 任务内容：
  - 题目渲染、评分计算、结果建议
- 预留路由：/quiz
- 验收标准：
  - 完成答题后可展示等级与建议

### FE-P1-02 案例列表与详情（一期）
- 任务内容：
  - 列表筛选、详情页结构化展示
- 预留路由：/cases、/cases/:id
- 验收标准：
  - 可筛选、可查看详情、可返回

### FE-P1-03 埋点接入
- 任务内容：
  - 页面访问、提交、查询、复制编号等关键事件埋点
- 验收标准：
  - 核心事件可在控制台/日志端验证触发

---

## 3.2 后端任务清单

### BE-P0-01 举报提交接口
- 接口：POST /api/reports
- 任务内容：
  - 参数校验
  - 生成唯一编号（AB-YYYYMMDD-XXXX）
  - 记录创建时间与初始状态
- 验收标准：
  - 并发下编号不重复
  - 非法参数返回标准错误码

### BE-P0-02 编号查询接口
- 接口：GET /api/reports/:id
- 任务内容：
  - 按编号返回详情、状态、时间线
  - 不存在返回 404 + 业务提示码
- 验收标准：
  - 查询耗时可控
  - 响应结构稳定

### BE-P0-03 状态流转接口（运营）
- 接口：PATCH /api/reports/:id/status
- 任务内容：
  - 状态机约束（待初审→处理中→已反馈）
  - 记录操作日志 updates
- 验收标准：
  - 非法流转被拦截
  - 历史记录可追溯

### BE-P0-04 最小风控能力
- 任务内容：
  - IP/设备频率限制（基础版）
  - 防刷策略预留
- 验收标准：
  - 高频提交可被限制并提示

### BE-P1-01 统计接口（一期）
- 接口：GET /api/metrics/overview
- 任务内容：
  - 提供累计线索、地区分布、话术占比
- 验收标准：
  - 与前端展示字段一致

### BE-P1-02 案例库接口（一期）
- 接口：GET /api/cases、GET /api/cases/:id
- 任务内容：
  - 支持分页与筛选
- 验收标准：
  - 分页准确，筛选字段有效

---

## 3.3 测试任务清单

### QA-P0-01 功能测试（主流程）
- 覆盖：
  - 提交成功
  - 查询成功
  - 查询不存在
  - 状态展示正确
- 验收标准：
  - 主流程 100% 通过

### QA-P0-02 异常与边界测试
- 覆盖：
  - 必填缺失、超长输入、特殊字符
  - 接口超时、500、网络断开
- 验收标准：
  - 页面无崩溃，提示可理解

### QA-P0-03 兼容与响应式测试
- 覆盖：
  - Chrome/Edge
  - 移动端常见宽度
- 验收标准：
  - 关键页面布局无错位

### QA-P1-01 回归测试与发布检查
- 覆盖：
  - 每周回归
  - 构建产物验证
  - 基础性能检查
- 验收标准：
  - 无阻塞缺陷进入发布

---

## 4. 接口契约草案（便于联调）

## 4.1 POST /api/reports
请求体：
- name: string
- location: string
- address: string
- description: string

响应体：
- id: string
- status: 待初审
- createdAt: string

## 4.2 GET /api/reports/:id
响应体：
- id
- name
- location
- address
- description
- status
- createdAt
- updates: [{ time, content, operator }]

## 4.3 PATCH /api/reports/:id/status
请求体：
- status: 待初审 | 处理中 | 已反馈
- note: string

响应体：
- id
- status
- updates

---

## 5. 里程碑排期（4 周）

## 第 1 周：闭环打通
- FE：表单 + 成功页 + 查询页
- BE：提交接口 + 查询接口
- QA：主流程测试用例

## 第 2 周：稳定与可用性
- FE：空态/错误态统一
- BE：状态流转 + 风控基础
- QA：异常/边界/兼容性

## 第 3 周：内容扩展（P1）
- FE：防骗自测、案例页骨架
- BE：统计与案例接口
- QA：功能回归

## 第 4 周：发布准备
- 全链路联调
- 性能与安全基线验证
- 发布与复盘

---

## 6. 缺陷分级与处理 SLA（建议）

- P0（阻塞）：核心流程不可用，24h 内修复
- P1（严重）：主要功能受影响，48h 内修复
- P2（一般）：体验问题，迭代内修复
- P3（建议）：优化项，排入后续版本

---

## 7. Definition of Done（研发版）

每项任务完成需满足：
1. 功能代码完成并自测通过
2. 联调通过，接口契约一致
3. 有对应测试用例与结果
4. 关键埋点已验证触发
5. 构建通过，无阻塞错误
6. 文档同步更新

---

## 8. 任务看板模板（研发执行）

| 任务ID | 端别 | 模块 | 任务描述 | 优先级 | 负责人 | 状态 | 工时(人天) | 验收标准 |
|---|---|---|---|---|---|---|---|---|
| FE-P0-01 | 前端 | 举报 | 表单校验与提交流程 | P0 |  | 待开始 | 1.5 | 校验完整、可防重复提交 |
| FE-P0-02 | 前端 | 查询 | 成功页与编号查询 | P0 |  | 待开始 | 1.5 | 可查询、无结果引导清晰 |
| BE-P0-01 | 后端 | 举报 | 提交接口与编号生成 | P0 |  | 待开始 | 1.0 | 编号唯一、响应稳定 |
| BE-P0-02 | 后端 | 查询 | 编号查询接口 | P0 |  | 待开始 | 0.8 | 结构一致、性能稳定 |
| QA-P0-01 | 测试 | 主流程 | 提交-查询全链路测试 | P0 |  | 待开始 | 1.0 | 主流程 100% 通过 |

---

## 9. 下一步执行建议

1. 先开评审会冻结 P0 字段和接口契约。
2. 建立每日站会同步风险与阻塞。
3. 每周固定版本回归与数据复盘。
4. 以“可用闭环优先”，避免过早扩项。

---

## 10. 单人研发模式（本项目适配版）

本项目为单人开发，建议将前端/后端/测试任务改为“同一人分时切换”，按固定顺序执行：

需求确认 → 页面与交互 → 接口与数据 → 自测与回归 → 发布复盘

### 10.1 单人任务池（统一优先级）

#### A 类：必须完成（P0）
1. FE：举报提交与查询闭环
2. BE：提交接口、查询接口、状态流转
3. QA：主流程 + 异常流程自测

#### B 类：建议完成（P1）
1. 防骗自测页
2. 案例列表/详情页
3. 埋点与基础看板

#### C 类：延后（P2）
1. 后台权限体系
2. 多渠道接入
3. 自动化报表

### 10.2 单人周节奏（推荐）
1. 周一：冻结本周 3 个必做任务
2. 周二~周四：开发 + 联调 + 自测
3. 周五：回归测试 + 构建发布 + 指标复盘

### 10.3 单人 DoD（完成定义）
每个任务完成必须同时满足：
1. 本地功能可用
2. `pnpm build` 通过
3. 关键异常路径已手测
4. 文档同步更新（至少更新任务状态）

### 10.4 单人风险控制
1. 每周在制任务不超过 3 项
2. 禁止同时开启两个大功能分支
3. 任何新需求先进入“下周候选”，不直接插队

---

## 11. 单人执行看板模板（轻量）

| 任务ID | 任务 | 优先级 | 状态 | 预计工时(人天) | 实际工时 | 风险/阻塞 | 备注 |
|---|---|---|---|---|---|---|---|
| SOLO-01 | 举报闭环完善 | P0 | 进行中 | 2.0 |  |  |  |
| SOLO-02 | 查询异常态补齐 | P0 | 待开始 | 1.0 |  |  |  |
| SOLO-03 | 防骗自测最小版 | P1 | 待开始 | 1.5 |  |  |  |

> 建议：每周只保留 3 条“进行中/待开始”任务，其他进入候选池。
